<h1>burgers!</h1>

<h2>Burgers That Are Ready To Eat!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers That You Have Eaten</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" burger_name="burger_name">
  </div>

  <div class="form-group">
    <label for="devoured">Eat now or Later??</label><br>
    <input type="radio" burger_name="devoured" value="1"> EAT NOW!<br>
    <input type="radio" burger_name="devoured" value="0"> I'll Save It For Later
  </div>

  <button type="submit">Add burger</button>
</form>
